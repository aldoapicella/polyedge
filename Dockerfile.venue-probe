FROM node:22-bookworm-slim

WORKDIR /app

COPY venue-probe/package.json venue-probe/package-lock.json ./
RUN npm ci --omit=dev --no-audit --no-fund

COPY venue-probe/src ./src

USER node

CMD ["node", "src/probe.mjs"]
