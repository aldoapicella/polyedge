log_level = "info"
domains = ["oidc.jupiterlabs.dev"]
listen_socket_path = "/run/spire-oidc/provider.sock"
jwt_issuer = "https://oidc.jupiterlabs.dev"
jwks_uri = "https://oidc.jupiterlabs.dev/keys"
set_key_use = true

workload_api {
    socket_path = "/run/spire/agent.sock"
    trust_domain = "polyedge.local"
}

health_checks {
    bind_port = "8008"
}
